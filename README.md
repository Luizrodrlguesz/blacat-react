# Blacat Next

Projeto migrado de React + Vite para Next.js com TypeScript.

## Rodando localmente

```bash
npm install
npm run dev
```

Abra `http://localhost:3000`.

## Rodando com Docker

```bash
docker compose up --build
```

Abra `http://localhost:3000`.
